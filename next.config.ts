import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // Turbopack est activé par défaut dans Next.js 16
  // Configuration vide pour silencer l'avertissement
  turbopack: {}
};

export default nextConfig;
